<h1>ログインページ</h1>
<%= form_with url: login_path do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>

  <%= form.label :password %>
  <%= form.password_field :password %>

  <%= form.submit t('.push') %>
<% end %>
<%= link_to "パスワードを忘れた場合こちらへ", new_password_reset_path %>